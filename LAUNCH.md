# LAUNCH

Replace the existing GitHub Pages files with every file in this folder. `index.html` must remain at the repository root.

GitHub Pages settings: Settings → Pages → Deploy from a branch → main → / (root).

After publishing, test: Home → Emperors → individual emperor → Enemies → individual enemy → Maps → era buttons → Search.
